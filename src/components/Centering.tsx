import styled from '@emotion/native';

export const Centering = styled.View({
  flex: 1,
  width: '100%',
  height: '100%',
  backgroundColor: '#fff',
  alignItems: 'center',
  justifyContent: 'center',
});
